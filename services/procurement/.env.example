# Локально піднімається через docker compose up postgres rabbitmq
DATABASE_URL=postgresql+asyncpg://baymeister:baymeister@localhost:5432/procurement

# Без шини закупівлі не бачать сигналів «мало на складі», а склад — приходів.
# RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# catalog напряму, всередині мережі.
CATALOG_URL=http://localhost:8004

# Рік у номері замовлення — за місцевим часом сервісу.
BUSINESS_TIMEZONE=Europe/Kyiv

PORT=8008
