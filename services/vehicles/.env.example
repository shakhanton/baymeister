# Локально піднімається через docker compose up postgres rabbitmq
DATABASE_URL=postgresql+asyncpg://baymeister:baymeister@localhost:5432/vehicles

# Якщо не задано — події пишуться в лог, а копія власника не оновлюється.
# RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Сервіс customers напряму, всередині мережі.
CUSTOMERS_URL=http://localhost:8001

PORT=8003
